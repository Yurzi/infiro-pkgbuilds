maintainers:
  - github: Yurzi
    email: yurzi@foxmail.com

build_prefix: extra-x86_64

pre_build_script: |
  aur_pre_build(maintainers=['taotieren'])

post_build_script: |
  aur_post_build

update_on:
  - source: aur
    aur: openlist
