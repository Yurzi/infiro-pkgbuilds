maintainers:
  - github: Yurzi
    email: yurzi@foxmail.com

build_prefix: extra-x86_64

repo_depends:
  - postgresql-pgvector

pre_build_script: |
  update_pkgver_and_pkgrel(_G.newver)

post_build_script: |
  git_pkgbuild_commit()

update_on:
  - source: manual
    manual: 0.4.3
  - source: alpm
    alpm: postgresql
